<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head") %>
<body>
  <%- include("../partials/nav") %>
  <main>
    <div class="form-container">
        <h1><%= editing ? "Edit your Home" : "Airbnb your home" %></h1>
        
        <form action="/host/<%= editing ? 'edit-home' : 'add-home' %>" method="POST">
            
            <% if (editing) { %>
                <input type="hidden" name="id" value="<%= home.id %>">
            <% } %>

            <div class="form-control">
                <label>Property Name</label>
                <input type="text" name="houseName" placeholder="e.g. Modern Loft" value="<%= editing ? home.houseName : '' %>" required>
            </div>
            
            <div class="form-control">
                <label>Price per Night (₹)</label>
                <input type="number" name="price" placeholder="5000" value="<%= editing ? home.price : '' %>" required>
            </div>
            
            <div class="form-control">
                <label>Location</label>
                <input type="text" name="location" placeholder="City" value="<%= editing ? home.location : '' %>" required>
            </div>
            
            <div class="form-control">
                <label>Rating</label>
                <input type="number" step="0.1" name="rating" placeholder="5.0" value="<%= editing ? home.rating : '' %>" required>
            </div>
            
            <div class="form-control">
                <label>Photo URL</label>
                <input type="text" name="photoUrl" placeholder="https://..." value="<%= editing ? home.photoUrl : '' %>" required>
            </div>
            
            <button class="submit-btn" type="submit">
                <%= editing ? "Update Home" : "Create Listing" %>
            </button>
        </form>
    </div>
  </main>
</body>
</html>