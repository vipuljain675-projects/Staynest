<%- include('../partials/head') %>
</head>
<body>
  <%- include('../partials/nav') %>

  <main class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4 border-bottom pb-3">
      <h1 class="text-primary fw-bold mb-0">Your Listings</h1>
      
      <a href="/host/add-home" class="btn btn-primary shadow-sm d-flex align-items-center">
        <i class="bi bi-plus-circle me-2"></i> Add New Home
      </a>
    </div>
    
    <div class="row">
      <% if (homes.length > 0) { %>
        <% for (let home of homes) { %>
          <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
              <div class="position-relative">
                <img src="<%= home.photoUrl %>" class="card-img-top" alt="<%= home.houseName %>" style="height: 200px; object-fit: cover;">
                <span class="position-absolute top-0 end-0 bg-dark text-white px-2 py-1 m-2 rounded opacity-75 small">
                  ⭐ <%= home.rating %>
                </span>
              </div>

              <div class="card-body d-flex flex-column">
                <div class="d-flex justify-content-between align-items-start mb-2">
                  <h5 class="card-title mb-0 fw-bold text-dark"><%= home.houseName %></h5>
                </div>
                <p class="text-muted small mb-2"><i class="bi bi-geo-alt-fill"></i> <%= home.location %></p>
                
                <p class="card-text text-danger fw-bold fs-5">₹<%= home.price %> <span class="fs-6 text-dark fw-normal">/ night</span></p>
                
                <div class="d-flex justify-content-between mt-3 pt-3 border-top">
                  <a href="/host/edit-home/<%= home._id %>?editing=true" class="btn btn-outline-secondary w-45 btn-sm">
                    <i class="bi bi-pencil-square"></i> Edit
                  </a>
                  
                  <form action="/host/delete-home/<%= home._id %>" method="POST" class="w-45">
                    <button type="submit" class="btn btn-outline-danger w-100 btn-sm">
                      <i class="bi bi-trash"></i> Delete
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% } %>
      <% } else { %>
        <div class="text-center w-100 mt-5 pt-5">
          <div class="display-1 text-muted mb-3">🏠</div>
          <h3 class="text-muted">No properties listed yet.</h3>
          <p class="mb-4">Start earning by listing your empty space today.</p>
          <a href="/host/add-home" class="btn btn-primary btn-lg shadow">Add Your First Home</a>
        </div>
      <% } %>
    </div>
  </main>
</body>
</html>