<nav class="navbar bg-white border-bottom sticky-top">
  <div class="container-fluid px-4 d-flex align-items-center gap-4">

  <!-- AIRBNB LOGO (OFFICIAL BELO STYLE) -->
<a href="/" class="airbnb-logo d-flex align-items-center gap-1">
  <svg
    width="32"
    height="32"
    viewBox="0 0 1000 1000"
    aria-hidden="true"
    style="display:block"
  >
    <path
      fill="#FF385C"
      d="M500 130c-106 0-192 86-192 192 0 163 192 415 192 415s192-252 192-415c0-106-86-192-192-192zm0 260c-38 0-68-30-68-68s30-68 68-68 68 30 68 68-30 68-68 68z"
    />
  </svg>

  <span class="airbnb-text">airbnb</span>
</a>


    <!-- SEARCH (ALWAYS VISIBLE) -->
<form action="/search" method="GET" class="flex-grow-1" style="max-width: 460px;">
      <div class="input-group shadow-sm border rounded-pill overflow-hidden">
        <input
          type="text"
          name="q"
          class="form-control border-0 shadow-none ps-4"
          placeholder="Search destinations"
          required
        >
       <button
  class="btn btn-danger rounded-circle m-1"
  style="width:34px;height:34px;"
  type="submit"
>

      </div>
    </form>

    <!-- RIGHT ACTIONS -->
    <div class="d-flex align-items-center gap-3">

      <a href="/homes" class="fw-semibold text-dark text-decoration-none">
        Homes
      </a>

      <% if (isAuthenticated) { %>

        <a href="/bookings" class="fw-semibold text-dark text-decoration-none">
          Trips
        </a>

        <a href="/favourite-list" class="fw-semibold text-dark text-decoration-none">
          Saved
        </a>

        <a href="/host"
           class="btn btn-light border rounded-pill px-3 fw-semibold">
          Switch to Host
        </a>

        <form action="/logout" method="POST" class="m-0">
          <button class="btn btn-outline-danger btn-sm rounded-pill px-3">
            Logout
          </button>
        </form>

      <% } else { %>

        <a href="/login" class="fw-semibold text-dark text-decoration-none">
          Log in
        </a>

        <a href="/signup"
           class="btn btn-dark rounded-pill px-3 fw-semibold">
          Sign up
        </a>

        <a href="/host"
           class="btn btn-light border rounded-pill px-3 fw-semibold">
          Switch to Host
        </a>

      <% } %>

    </div>

  </div>
</nav>
